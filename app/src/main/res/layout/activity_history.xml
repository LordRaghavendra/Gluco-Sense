<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/surface">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/surface"
        android:titleTextAppearance="@style/TextAppearance.HealthTracker.Title"
        app:title="@string/title_history"
        app:navigationIcon="@android:drawable/ic_menu_revert"
        app:navigationIconTint="@color/on_surface" />

    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tabs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:tabMode="scrollable"
        app:tabTextAppearance="@style/TextAppearance.HealthTracker.Body"
        app:tabIndicatorColor="@color/brand_primary"
        app:tabSelectedTextColor="@color/brand_primary"
        app:tabTextColor="@color/on_surface_muted">

        <com.google.android.material.tabs.TabItem
            android:text="@string/tab_glucose" />
        <com.google.android.material.tabs.TabItem
            android:text="@string/tab_bp" />
        <com.google.android.material.tabs.TabItem
            android:text="@string/tab_medicine" />
        <com.google.android.material.tabs.TabItem
            android:text="@string/tab_chart" />
    </com.google.android.material.tabs.TabLayout>

    <FrameLayout
        android:id="@+id/content"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="@dimen/screen_padding"
            android:clipToPadding="false" />

        <com.github.mikephil.charting.charts.LineChart
            android:id="@+id/chart"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="@dimen/screen_padding"
            android:visibility="gone" />

        <TextView
            android:id="@+id/txtEmpty"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/msg_no_history"
            android:textAppearance="@style/TextAppearance.HealthTracker.BodyLarge"
            android:textColor="@color/on_surface_muted"
            android:gravity="center"
            android:padding="40dp"
            android:visibility="gone" />
    </FrameLayout>
</LinearLayout>
